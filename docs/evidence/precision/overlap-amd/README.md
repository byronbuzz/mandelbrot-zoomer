# Bounded perturbation overlap evidence

The exact user-selected complex boundary was evaluated at approximately `10^5.1` magnification with a 5,000-iteration target on the AMD adapter.

Observed result:

- four bounded focus tiles activated perturbation;
- reference demand drained to zero after several obsolete camera epochs were cancelled;
- zero reference failures and zero precision-limited overlap tiles;
- 224-bit reference working arithmetic and explicitly declared 96-bit GPU orbit transport;
- zero WebGPU validation or browser errors;
- the direct and perturbation production shader hashes remained unchanged.

The policy oracle also proves that the current transport is sufficient for the overlap fixture but insufficient at a representative `10^35` sample exponent. This stage does not claim `10^35` authority; it makes that ceiling explicit and executable.

Raw evidence is in `report.json`.
