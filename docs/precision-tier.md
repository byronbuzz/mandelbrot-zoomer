# Precision tier

The renderer uses the original f32 path below 10,000x magnification and switches automatically to paired-f32 double-float coordinate and orbit arithmetic at deeper zoom levels. The UI reports the active tier and completed GPU frame rate.
