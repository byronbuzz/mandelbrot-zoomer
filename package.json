{
  "name": "mandelbrot-zoomer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate:wgsl": "node scripts/validate-wgsl-source.mjs",
    "build": "npm run validate:wgsl && tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --pretty false"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.64",
    "typescript": "^5.9.2",
    "vite": "^7.1.1"
  }
}
